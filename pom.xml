<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>cz.bliksoft.meshcorecompanion</groupId>
	<artifactId>bsmeshcorecompanion-desktop</artifactId>
	<version>${revision}</version>

	<properties>
		<revision>0.1.4</revision>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<maven.compiler.release>21</maven.compiler.release>
		<main.class>cz.bliksoft.meshcorecompanion.Main</main.class>
		<skipGitInfo>false</skipGitInfo>
		<!-- Target platform of the distribution zip = JavaFX's native classifier, which the OpenJFX
		     poms pick from the build machine; the platform-* profiles below mirror that for the zip
		     name. For another platform override it (the OpenJFX poms read the same property):
		       mvn package -Djavafx.platform=linux
		     (win, linux, linux-aarch64, mac, mac-aarch64). -->
		<javafx.platform>win</javafx.platform>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>cz.bliksoft.java</groupId>
				<artifactId>dependency-management</artifactId>
				<version>1.21.2</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<groupId>cz.bliksoft.java</groupId>
			<artifactId>dependency-management-log4j</artifactId>
			<type>pom</type>
		</dependency>

		<dependency>
			<groupId>cz.bliksoft.java</groupId>
			<artifactId>common-java-utils-jfx</artifactId>
			<!-- 0.6: WebView-free About dialog with the lib_credits of the dependencies,
			     javafx-web optional, JUnit only in test scope. -->
			<version>0.6</version>
		</dependency>

		<dependency>
			<groupId>cz.bliksoft.meshcore</groupId>
			<artifactId>meshcore-companion</artifactId>
			<version>0.3.0</version>
		</dependency>

		<!-- https://github.com/Fazecast/jSerialComm -->
		<dependency>
			<groupId>com.fazecast</groupId>
			<artifactId>jSerialComm</artifactId>
		</dependency>

		<dependency>
			<groupId>cz.bliksoft.java</groupId>
			<artifactId>common-java-utils-ble</artifactId>
			<version>0.7.0</version>
		</dependency>

		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
			<version>2.18.2</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.13.0</version>
				<configuration>
					<release>${maven.compiler.release}</release>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.openjfx</groupId>
				<artifactId>javafx-maven-plugin</artifactId>
				<version>0.0.8</version>
				<configuration>
					<mainClass>${main.class}</mainClass>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>3.4.2</version>
				<configuration>
					<archive>
						<manifest>
							<mainClass>${main.class}</mainClass>
							<addClasspath>true</addClasspath>
							<classpathPrefix>lib/</classpathPrefix>
						</manifest>
					</archive>
				</configuration>
			</plugin>

			<!-- assemble distributable zip: app.jar + lib + scripts + config + icons -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-assembly-plugin</artifactId>
				<version>3.7.1</version>
				<executions>
					<execution>
						<id>make-zip</id>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
						<configuration>
							<appendAssemblyId>false</appendAssemblyId>
							<finalName>${project.artifactId}-${project.version}-${javafx.platform}</finalName>
							<descriptors>
								<descriptor>src/assembly/app.xml</descriptor>
							</descriptors>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>io.github.git-commit-id</groupId>
				<artifactId>git-commit-id-maven-plugin</artifactId>
				<version>9.0.2</version>
				<executions>
					<execution>
						<id>get-the-git-infos</id>
						<goals>
							<goal>revision</goal>
						</goals>
						<phase>initialize</phase>
					</execution>
				</executions>
				<configuration>
					<dotGitDirectory>${maven.multiModuleProjectDirectory}/.git</dotGitDirectory>
					<generateGitPropertiesFile>true</generateGitPropertiesFile>
					<generateGitPropertiesFilename>
						${project.build.outputDirectory}/git.properties
					</generateGitPropertiesFilename>
					<commitIdGenerationMode>full</commitIdGenerationMode>
					<failOnNoGitDirectory>false</failOnNoGitDirectory>
					<failOnUnableToExtractRepoInfo>false</failOnUnableToExtractRepoInfo>
					<skip>${skipGitInfo}</skip>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>build-helper-maven-plugin</artifactId>
				<version>3.5.0</version>
				<executions>
					<execution>
						<id>add-generated-sources</id>
						<phase>generate-sources</phase>
						<goals>
							<goal>add-source</goal>
						</goals>
						<configuration>
							<sources>
								<source>${project.build.directory}/generated-sources/versioninfo</source>
							</sources>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>templating-maven-plugin</artifactId>
				<version>3.0.0</version>
				<executions>
					<execution>
						<goals>
							<goal>filter-sources</goal>
						</goals>
						<phase>generate-sources</phase>
					</execution>
				</executions>
				<configuration>
					<outputDirectory>
						${project.build.directory}/generated-sources/versioninfo
					</outputDirectory>
				</configuration>
			</plugin>
			<plugin>
				<groupId>net.revelc.code.formatter</groupId>
				<artifactId>formatter-maven-plugin</artifactId>
				<version>2.24.1</version>
				<configuration>
					<encoding>UTF-8</encoding>
					<configFile>src/formatter/EclipseFormatter.xml</configFile>
				</configuration>
			</plugin>
		</plugins>
	</build>
	<profiles>
		<!-- javafx.platform of the build machine (see the property); -Djavafx.platform wins -->
		<profile>
			<id>platform-linux</id>
			<activation>
				<os>
					<name>linux</name>
					<arch>amd64</arch>
				</os>
			</activation>
			<properties>
				<javafx.platform>linux</javafx.platform>
			</properties>
		</profile>
		<profile>
			<id>platform-linux-aarch64</id>
			<activation>
				<os>
					<name>linux</name>
					<arch>aarch64</arch>
				</os>
			</activation>
			<properties>
				<javafx.platform>linux-aarch64</javafx.platform>
			</properties>
		</profile>
		<profile>
			<id>platform-mac</id>
			<activation>
				<os>
					<family>mac</family>
					<arch>x86_64</arch>
				</os>
			</activation>
			<properties>
				<javafx.platform>mac</javafx.platform>
			</properties>
		</profile>
		<profile>
			<id>platform-mac-aarch64</id>
			<activation>
				<os>
					<family>mac</family>
					<arch>aarch64</arch>
				</os>
			</activation>
			<properties>
				<javafx.platform>mac-aarch64</javafx.platform>
			</properties>
		</profile>
		<!-- mvn package -Pdist: a zip for every platform (for a release) instead of just
		     javafx.platform's - one build, the JavaFX jars of each platform copied next to it
		     (the classifier variants of the resolved org.openjfx dependencies). -->
		<profile>
			<id>dist</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-dependency-plugin</artifactId>
						<version>3.6.1</version>
						<configuration>
							<includeScope>runtime</includeScope>
							<includeGroupIds>org.openjfx</includeGroupIds>
						</configuration>
						<executions>
						<execution>
							<id>javafx-win</id>
							<phase>prepare-package</phase>
							<goals>
								<goal>copy-dependencies</goal>
							</goals>
							<configuration>
								<classifier>win</classifier>
								<outputDirectory>${project.build.directory}/dist/win/lib</outputDirectory>
							</configuration>
						</execution>
						<execution>
							<id>javafx-linux</id>
							<phase>prepare-package</phase>
							<goals>
								<goal>copy-dependencies</goal>
							</goals>
							<configuration>
								<classifier>linux</classifier>
								<outputDirectory>${project.build.directory}/dist/linux/lib</outputDirectory>
							</configuration>
						</execution>
						<execution>
							<id>javafx-linux-aarch64</id>
							<phase>prepare-package</phase>
							<goals>
								<goal>copy-dependencies</goal>
							</goals>
							<configuration>
								<classifier>linux-aarch64</classifier>
								<outputDirectory>${project.build.directory}/dist/linux-aarch64/lib</outputDirectory>
							</configuration>
						</execution>
						<execution>
							<id>javafx-mac</id>
							<phase>prepare-package</phase>
							<goals>
								<goal>copy-dependencies</goal>
							</goals>
							<configuration>
								<classifier>mac</classifier>
								<outputDirectory>${project.build.directory}/dist/mac/lib</outputDirectory>
							</configuration>
						</execution>
						<execution>
							<id>javafx-mac-aarch64</id>
							<phase>prepare-package</phase>
							<goals>
								<goal>copy-dependencies</goal>
							</goals>
							<configuration>
								<classifier>mac-aarch64</classifier>
								<outputDirectory>${project.build.directory}/dist/mac-aarch64/lib</outputDirectory>
							</configuration>
						</execution>
						</executions>
					</plugin>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-assembly-plugin</artifactId>
						<executions>
							<execution>
								<id>make-zip</id>
								<phase>none</phase>
							</execution>
							<execution>
								<id>make-dist-zips</id>
								<phase>package</phase>
								<goals>
									<goal>single</goal>
								</goals>
								<configuration>
									<finalName>${project.artifactId}-${project.version}</finalName>
									<descriptors>
								<descriptor>src/assembly/dist/win.xml</descriptor>
								<descriptor>src/assembly/dist/linux.xml</descriptor>
								<descriptor>src/assembly/dist/linux-aarch64.xml</descriptor>
								<descriptor>src/assembly/dist/mac.xml</descriptor>
								<descriptor>src/assembly/dist/mac-aarch64.xml</descriptor>
									</descriptors>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>
